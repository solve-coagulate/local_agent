"""Smoke test to confirm the test runner is configured."""


def test_hello_world() -> None:
    """A simple assertion that doubles as a hello world test."""

    greeting = "Hello, World!"
    assert greeting == "Hello, World!"
